# Flask
SECRET_KEY=change-this-to-a-random-value
FLASK_ENV=development

# MySQL connection for the main app database
DB_HOST=localhost
DB_PORT=3306
DB_USER=expense_tracker
DB_PASSWORD=changeme
DB_NAME=expense_tracker

# Separate MySQL database used only when running pytest
TEST_DB_NAME=expense_tracker_test
